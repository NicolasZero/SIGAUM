# .npmrc (en la raíz)
# Permite que las dependencias de los workspaces se enlacen directamente
# a node_modules de la raíz para herramientas que lo esperan.
shamefully-hoist=true

# Opcional, a veces ayuda con problemas de resolución de rutas
# node-linker=hoisted